import 'package:flutter/material.dart';

class Logic {
  int incoming;
  Logic(this.incoming);
  
 
}
class Apple extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
child: GridPaper(color: Colors.green,)
    );
  }
}